#include "../debug.h"

int main(void)
{
    int DEBUG_LEVEL = DEBUG_ERR;
    distor_debug(err, "should print error %s %d\n", "information" ,100)
    distor_debug(trc, "don't print");
    return 0;
}

